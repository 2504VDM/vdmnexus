# Daily review — 2026-09-10

Nightly daily-review loop (`prompts/00-daily-review.md`).

## 0. Slack feedback

Read `#nexus` back through 30 messages (to 2026-08-10 22:18 CEST). Every
message in that window is this loop's own nightly post (posted under
Dennis's account, tagged "Sent using @claude"); zero human replies
anywhere, including on last night's post (2026-09-09 22:13 CEST).
Nothing to apply.

## 1-2. Gather + review

**Process integrity — fifth clean night confirmed, tonight is the sixth
attempt.** `pull_request_read`/commit history confirms #196
(2026-09-09's review) merged cleanly: opened 20:13:22 UTC, merged
20:13:31 UTC — 9 seconds — despite `mergeable_state: unstable` (the
`nexus` Vercel check is still failing). That's five consecutive clean
nights under the corrected merge logic (#192, #193, #194, #195, #196).
Tonight's own PR is being opened and merged the same way — see
`STATUS.md` for confirmation once it lands.

**No new open or merged PRs since #196.** `main` is current. #106
(cards-v1 spec, merge-or-close decision) and #95 (Polymarket agent,
blocked on Spanish counsel) are the only two other open PRs, unchanged,
now 109 and 110 days open respectively. No product code has merged
since #162 (2026-08-06) — 35 days.

**Deposit-crediting bug — still live, unresolved, 20 days.** Reconfirmed
fresh tonight against live production runtime logs (`nexus` project,
last 2h window, `deposits/scan` cron): every single run in the window
(16/16 invocations, 19:30-20:08 UTC) still hits `RPC getTransaction
HTTP 429` on the same three stuck Solana signatures and completes with
`credited:0`. 20 days since 2026-08-21. Still needs Dennis's decision on
the RPC provider/rate limit — this is not a code-level bug this loop can
fix without that call.

**`nexus` Vercel project's failing check — reconfirmed fresh, not carried
forward.** `list_teams` shows `vdm-nexus` still on the Hobby plan.
`list_deployments` for the `nexus` project shows no deployment newer than
2026-08-19T20:13 UTC (same production deployment `dpl_kMXumydsSvke6TjRGXWJZXbdqA3g`
serving every deposit-scan request tonight) — still the root cause behind
`mergeable_state: unstable` on every planning PR. Unresolved, 20 days
since first flagged (2026-08-21). Separate ask from the deposit-crediting
bug even though both trace back to the same Solana/Vercel surface.

**Health checks.** All three attempted directly tonight via `curl`
through the session's egress proxy:
- `POST https://www.vdmnexus.com/api/playground/inference` — connection
  rejected by the egress proxy (`connect_rejected`, organization policy).
- `https://verify.vdmnexus.com` — same, `connect_rejected`.
- `https://nexus.vdmnexus.com/api/health` — same, `connect_rejected`.

42nd consecutive confirmed night this session's network policy blocks
all three hosts. Needs Dennis's call: allowlist these hosts for the
scheduled session's egress policy, or move health checks elsewhere.

**Launch readiness** (`marketing/token-launch-checklist.md`): the T-14 /
T-48h / T-0 steps still describe Solana tooling (pump.fun, Squads,
Solscan, Bubblemaps Solana) and need a rewrite pass for the Uniswap v4 /
Robinhood Chain venue. 42 days unpicked since the 2026-07-30 re-venue.
Reusable building blocks already merged: #156 (wallet connect, chain IDs
46630/4663), #160 (`/live` chain reads), #162 (trustless vault
enumeration).

**Rienda M1-M5.** No new report from Dennis. Last known status
(2026-07-31: spec complete; token + Uniswap v4 fee-burn hook contracts,
26 passing tests; M1 in development) is now 41 days stale — well past
the 7-day threshold, escalating to a direct ask again.

No kill-criteria (`CLAUDE.md`, `marketing/token-launch-checklist.md`)
tripped tonight.

## 3. Tomorrow's plan

See `planning/NEXT.md`.

full plan: this file
