# Daily review — 2026-09-08

Nightly daily-review loop (`prompts/00-daily-review.md`).

## 0. Slack feedback

Read `#nexus` back through 15 messages (to 2026-08-24). Every message in
that window is this loop's own nightly post (posted under Dennis's
account, tagged "Sent using @claude"); zero human replies anywhere,
including on last night's post (2026-09-07 22:12 CEST). Nothing to apply.

## 1-2. Gather + review

**Process integrity — third clean night confirmed.** `pull_request_read`
on #194 (2026-09-07 review) shows `merged: true`, opened 20:12:16 UTC and
merged 20:12:24 UTC — 8 seconds, despite `mergeable_state: unstable` (the
`nexus` Vercel check is still failing). That's three consecutive clean
nights under the corrected merge logic (#192, #193, #194). Tonight's own
PR is being opened and merged the same way — see `STATUS.md` for
confirmation once it lands.

**No new open or merged PRs since #194.** #106 (cards-v1 spec,
merge-or-close decision) and #95 (Polymarket agent, blocked on Spanish
counsel) are the only two open PRs, unchanged, now 107 and 108 days open
respectively. No product code has merged since #162 (2026-08-06) — 33
days.

**Deposit-crediting bug — still live, unresolved, 18 days.** Carried
forward unchanged from last night's report (production
`/api/v1/deposits/scan` hitting `RPC getTransaction HTTP 429` on the same
three stuck Solana signatures, `credited:0` every run). 18 days since
2026-08-21. This session's read-only tooling cannot query Vercel runtime
logs directly (no Vercel MCP access in this environment) — tonight's
report carries forward the last confirmed runtime-log check rather than
a fresh one. Still needs Dennis's decision on the RPC provider/rate
limit.

**Health checks.** All three attempted directly tonight via `curl`
through the session's egress proxy:
- `POST https://www.vdmnexus.com/api/playground/inference` — connection
  rejected by the egress proxy (`connect_rejected`, organization policy).
- `https://verify.vdmnexus.com` — same, `connect_rejected`.
- `https://nexus.vdmnexus.com/api/health` — same, `connect_rejected`.

40th consecutive confirmed night this session's network policy blocks
all three hosts. Needs Dennis's call: allowlist these hosts for the
scheduled session's egress policy, or move health checks elsewhere.

**`nexus` Vercel project's failing check.** Still the root cause behind
`mergeable_state: unstable` on every planning PR (confirmed again on
#194 above). Unresolved, 18 days since first flagged (2026-08-21).
Separate ask from the deposit-crediting bug even though both trace back
to the same Solana/Vercel surface.

**Launch readiness** (`marketing/token-launch-checklist.md`): the T-14 /
T-48h / T-0 steps still describe Solana tooling (pump.fun, Squads,
Solscan, Bubblemaps Solana) and need a rewrite pass for the Uniswap v4 /
Robinhood Chain venue. 40 days unpicked since the 2026-07-30 re-venue.
Reusable building blocks already merged: #156 (wallet connect, chain IDs
46630/4663), #160 (`/live` chain reads), #162 (trustless vault
enumeration).

**Rienda M1-M5.** No new report from Dennis. Last known status
(2026-07-31: spec complete; token + Uniswap v4 fee-burn hook contracts,
26 passing tests; M1 in development) is now 39 days stale — well past
the 7-day threshold, escalating to a direct ask again.

No kill-criteria (`CLAUDE.md`, `marketing/token-launch-checklist.md`)
tripped tonight.

## 3. Tomorrow's plan

See `planning/NEXT.md`.

full plan: this file
