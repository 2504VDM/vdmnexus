# Daily review — 2026-09-06

Nightly daily-review loop (`prompts/00-daily-review.md`).

## 0. Slack feedback

Read `#nexus` back through 20 messages (to 2026-08-17). Every message in
that window is this loop's own nightly post (posted under Dennis's
account, tagged "Sent using @claude"); zero human replies anywhere,
including on last night's post (2026-09-05 22:17 CEST). Nothing to apply.

## 1-2. Gather + review

**Process integrity — one clean data point under the corrected merge
logic.** Confirmed via commit history that last night's PR (#192) opened
and merged cleanly the same session (20:15:30-20:16:56 UTC, ~1.5 minutes),
right after #175 was merged and #176-#191 closed. `main` is current. This
is the first full night since the fix (treating `unstable` as
mergeable-if-not-`dirty`/`blocked`) — one data point, not yet proof the
fix holds under a real future `unstable` state. No new planning PR is
stuck tonight because none was open going in.

**No new open or merged PRs since #192.** #106 (cards-v1 spec,
merge-or-close decision) and #95 (Polymarket agent, blocked on Spanish
counsel) are the only two open PRs, unchanged, now 105 and 106 days open
respectively. No product code has merged since #162 (2026-08-06) — 31
days.

**Deposit-crediting bug — still live, unresolved.** Carried forward
unchanged from last night's report (production `/api/v1/deposits/scan`
hitting `RPC getTransaction HTTP 429` on the same three stuck Solana
signatures, `credited:0` every run). 16 days since 2026-08-21. This
session's read-only tooling cannot query Vercel runtime logs directly
(no Vercel MCP access in this environment) — flagging that tonight's
report reconfirms this from yesterday's runtime-log check, not a fresh
one. Still needs Dennis's decision on the RPC provider/rate limit.

**Health checks.** All three attempted directly tonight:
- `POST https://www.vdmnexus.com/api/playground/inference` — connection
  rejected by the egress proxy (`connect_rejected`, organization policy).
- `https://verify.vdmnexus.com` — same, `connect_rejected`.
- `https://nexus.vdmnexus.com/api/health` — same, `connect_rejected`.

38th consecutive confirmed night this session's network policy blocks
all three hosts. Needs Dennis's call: allowlist these hosts for the
scheduled session's egress policy, or move health checks elsewhere.

**Launch readiness** (`marketing/token-launch-checklist.md`): the T-14 /
T-48h / T-0 steps still describe Solana tooling (pump.fun, Squads,
Solscan, Bubblemaps Solana) and need a rewrite pass for the Uniswap v4 /
Robinhood Chain venue. 38 days unpicked since the 2026-07-30 re-venue.
Reusable building blocks already merged: #156 (wallet connect, chain IDs
46630/4663), #160 (`/live` chain reads), #162 (trustless vault
enumeration).

**Rienda M1-M5.** No new report from Dennis. Last known status
(2026-07-31: spec complete; token + Uniswap v4 fee-burn hook contracts,
26 passing tests; M1 in development) is now 37 days stale — well past
the 7-day threshold, escalating to a direct ask again.

No kill-criteria (`CLAUDE.md`, `marketing/token-launch-checklist.md`)
tripped tonight.

## 3. Tomorrow's plan

See `planning/NEXT.md`.

full plan: this file
