# Daily review — 2026-09-07

Nightly daily-review loop (`prompts/00-daily-review.md`).

## 0. Slack feedback

Read `#nexus` back through 20 messages (to 2026-08-18). Every message in
that window is this loop's own nightly post (posted under Dennis's
account, tagged "Sent using @claude"); zero human replies anywhere,
including on last night's post (2026-09-06 22:13 CEST). Nothing to apply.

## 1-2. Gather + review

**Process integrity — second clean night confirmed.** Commit history
shows PR #193 (2026-09-06 review) opened and merged cleanly the same
session (20:11:29-20:12:38 UTC, ~68 seconds), right after #192 the night
before. That's two consecutive clean nights under the corrected merge
logic (treating `unstable` as mergeable-if-not-`dirty`/`blocked`). No new
planning PR was stuck tonight going in. Tonight's own PR is being opened
and merged the same way — see `STATUS.md` for confirmation once it lands
next session.

**No new open or merged PRs since #193.** #106 (cards-v1 spec,
merge-or-close decision) and #95 (Polymarket agent, blocked on Spanish
counsel) are the only two open PRs, unchanged, now 106 and 107 days open
respectively. No product code has merged since #162 (2026-08-06) — 32
days.

**Deposit-crediting bug — still live, unresolved.** Carried forward
unchanged from last night's report (production `/api/v1/deposits/scan`
hitting `RPC getTransaction HTTP 429` on the same three stuck Solana
signatures, `credited:0` every run). 17 days since 2026-08-21. This
session's read-only tooling cannot query Vercel runtime logs directly (no
Vercel MCP access in this environment) — tonight's report carries forward
the last confirmed runtime-log check rather than a fresh one. Still needs
Dennis's decision on the RPC provider/rate limit.

**Health checks.** All three attempted directly tonight:
- `POST https://www.vdmnexus.com/api/playground/inference` — connection
  rejected by the egress proxy (`connect_rejected`, organization policy).
- `https://verify.vdmnexus.com` — same, `connect_rejected`.
- `https://nexus.vdmnexus.com/api/health` — same, `connect_rejected`.

39th consecutive confirmed night this session's network policy blocks
all three hosts. Needs Dennis's call: allowlist these hosts for the
scheduled session's egress policy, or move health checks elsewhere.

**Launch readiness** (`marketing/token-launch-checklist.md`): the T-14 /
T-48h / T-0 steps still describe Solana tooling (pump.fun, Squads,
Solscan, Bubblemaps Solana) and need a rewrite pass for the Uniswap v4 /
Robinhood Chain venue. 39 days unpicked since the 2026-07-30 re-venue.
Reusable building blocks already merged: #156 (wallet connect, chain IDs
46630/4663), #160 (`/live` chain reads), #162 (trustless vault
enumeration).

**Rienda M1-M5.** No new report from Dennis. Last known status
(2026-07-31: spec complete; token + Uniswap v4 fee-burn hook contracts,
26 passing tests; M1 in development) is now 38 days stale — well past
the 7-day threshold, escalating to a direct ask again.

No kill-criteria (`CLAUDE.md`, `marketing/token-launch-checklist.md`)
tripped tonight.

## 3. Tomorrow's plan

See `planning/NEXT.md`.

full plan: this file
